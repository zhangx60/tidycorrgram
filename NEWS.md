# tidycorrgram 0.1.0

* Initial release.
* Added `corrgram_data()` for tidy correlation matrix calculation.
* Added `corrgram()` for `ggplot2` correlograms.
* Added `corrgram_palette()` for reusable diverging palettes.
